extends ./base

block header
  header.header
    h1= 'Kitchen'

block content
  article.content
    div.recipe-list
      each recipe in recipes
        div.recipe-item.link-container
          amp-img.recipe-thumbnail(
            src=recipe.image.url alt=recipe.name
            width=144 height=144
          )
          .description
            h2= recipe.name
            p= recipe.description
          a(href=`${dashify(recipe.name)}.html`)
            span.link-spanner
